#include <iostream>
#include <math.h>
#include <iomanip>
#include <fstream>
#include <limits>

int runge_exact(double* x, double* f);

int abs_exact(double* x, double* f);

int step_exact(double* x, double* f);

int main(int argc, char *argv[]) {


    return 0;
}